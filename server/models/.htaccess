# Dossier interne : aucun acces direct depuis le web.
# Les fichiers sont inclus par PHP cote serveur, jamais telecharges.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
